# ============================================
# Конфигурация для системных скриптов
# ============================================
# Этот файл используется bash скриптами внутри VM
# Скопируйте как vm/env для использования

# ============================================
# Git проект (опционально)
# ============================================

# URL Git репозитория для клонирования
# Пример публичного: https://github.com/username/project.git
# Пример приватного с токеном: https://oauth2:TOKEN@github.com/username/project.git
GIT_FULL_URL=""

# Путь к скрипту установки в склонированном проекте
# (относительно корня проекта, например: install.sh или scripts/setup.sh)
INSTALL_SCRIPT=""

# ============================================
# SSHFS подключение (опционально)
# ============================================

# Формат: user@host:/remote/path /local/mount/point
# Пример: root@192.168.1.100:/var/www /home/vagrant/remote
SSHFS_URL=""

# Пароль для SSHFS (если не используются SSH ключи)
SSH_PASSWORD=""
